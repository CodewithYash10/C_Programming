#include<stdio.h>
int main()
{
    int p,q;
    printf("Enter the value of p and q: ");
    scanf("%d%d",&p,&q);

    // int sum = p + q;
    // printf("Sum of Two number is: %d",sum);

    printf("p=%d , q=%d",p,q);


    return 0;
}