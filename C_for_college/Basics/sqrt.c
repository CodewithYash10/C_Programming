#include<stdio.h>
#include<math.h>
int main(){
    float num,result;
    printf("Enter a num");
    scanf("%f",&num);
    result = sqrt(num);
    printf("The Sqrt of given number is %f",result);

    return 0;
}